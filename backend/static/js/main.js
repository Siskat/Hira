var Nav = React.createClass({
      render: function() {
        return (
          <div>
            HELLO
          </div>
        );
      }
    });

    ReactDOM.render(
      React.createElement(Nav, null),
      document.getElementById('main')
    );